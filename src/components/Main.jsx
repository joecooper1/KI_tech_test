import React from "react";

import { MainView, ArticleList } from "../styles/Main";

export default function Main(props) {
  return (
    <MainView>
      <ArticleList></ArticleList>
    </MainView>
  );
}
